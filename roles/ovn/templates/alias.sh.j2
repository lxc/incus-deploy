# Managed by Ansible, do not modify.
alias ovn-nbctl="/usr/bin/ovn-nbctl --db={{ ovn_central_northbound }} -c /etc/ovn/{{ ovn_name }}.server.crt -p /etc/ovn/{{ ovn_name }}.server.key -C /etc/ovn/{{ ovn_name }}.ca.crt"
alias ovn-sbctl="/usr/bin/ovn-sbctl --db={{ ovn_central_southbound }} -c /etc/ovn/{{ ovn_name }}.server.crt -p /etc/ovn/{{ ovn_name }}.server.key -C /etc/ovn/{{ ovn_name }}.ca.crt"
{% if ovn_ic_northbound %}
alias ovn-ic-nbctl="/usr/bin/ovn-ic-nbctl --db={{ ovn_ic_northbound }} -c /etc/ovn/{{ ovn_name }}.server.crt -p /etc/ovn/{{ ovn_name }}.server.key -C /etc/ovn/{{ ovn_name }}.ca.crt"
{% endif %}
{% if ovn_ic_southbound %}
alias ovn-ic-sbctl="/usr/bin/ovn-ic-sbctl --db={{ ovn_ic_southbound }} -c /etc/ovn/{{ ovn_name }}.server.crt -p /etc/ovn/{{ ovn_name }}.server.key -C /etc/ovn/{{ ovn_name }}.ca.crt"
{% endif %}
